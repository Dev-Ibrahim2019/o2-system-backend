interface:
  display_name: "O2 System Backend"
  short_description: "Repo-specific patterns and workflows for o2-system-backend"
  default_prompt: "Use the o2-system-backend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true